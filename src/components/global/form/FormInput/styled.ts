import styled from 'styled-components';

export const StyledInput = styled.input`
    background: #FFFFFF;
    border: 1px solid rgba(11, 201, 31, 0.5);
    box-sizing: border-box;
    border-radius: 3px;

    font-weight: 300;
    font-size: 18px;
    line-height: 24px;
    padding: 14px 20px;
`;